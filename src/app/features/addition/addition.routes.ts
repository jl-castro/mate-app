import { Routes } from '@angular/router';
import { AdditionQuizComponent } from './components/quiz';

export const additionRoutes: Routes = [
  {
    path: '',
    component: AdditionQuizComponent,
  }
];
