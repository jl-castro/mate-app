import { Routes } from '@angular/router';
import { DivisionQuizComponent } from './components/quiz';

export const divisionRoutes: Routes = [
  {
    path: '',
    component: DivisionQuizComponent,
  }
];
